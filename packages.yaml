network-manager-applet:
        merge:
                - gnome-git
NetworkManager-libreswan:
        upstream: network-manager-libreswan
        merge:
                - gnome-git
NetworkManager-fortisslvpn:
        upstream: network-manager-fortisslvpn
        merge:
                - gnome-git
NetworkManager-vpnc:
        upstream: network-manager-vpnc
        merge:
                - gnome-git
NetworkManager-openvpn:
        upstream: network-manager-openvpn
        merge:
                - gnome-git
NetworkManager-pptp:
        upstream: network-manager-pptp
        merge:
                - gnome-git
NetworkManager:
        owner: NetworkManager
        merge:
                - freedesktop-git
libndp:
        owner: jpirko
        merge:
                - github-git
libteam:
        owner: jpirko
        merge:
                - github-git
libreswan:
        merge:
                - github-git
libqmi:
        merge:
                - freedesktop-git
libmbim:
        owner: libmbim
        ac_missing: yep
        merge:
                - freedesktop-git
ModemManager:
        owner: ModemManager
        merge:
                - freedesktop-git

NetworkManager-strongswan:
        upstream: strongswan
        owner: strongswan
        path: /src/frontends/gnome
        merge:
                - github-git
